/**
 * Display connection request status
 * Tracks the approval workflow for new display registrations
 */
export declare enum ConnectionRequestStatus {
    PENDING = "pending",
    APPROVED = "approved",
    REJECTED = "rejected"
}
//# sourceMappingURL=ConnectionRequestStatus.d.ts.map